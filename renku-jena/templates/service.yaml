apiVersion: v1
kind: Service
metadata:
  name: {{ include "renku-jena.fullname" . }}
  labels:
    {{- include "renku-jena.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "renku-jena.selectorLabels" . | nindent 4 }}
